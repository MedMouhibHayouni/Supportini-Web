import { NosProduitsPipe } from './nos-produits.pipe';

describe('NosProduitsPipe', () => {
  it('create an instance', () => {
    const pipe = new NosProduitsPipe();
    expect(pipe).toBeTruthy();
  });
});
